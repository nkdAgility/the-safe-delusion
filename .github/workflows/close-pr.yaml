# This workflow is managed by gh actions-lock.

name: Close the-safe-delusion PR preview
on:
  pull_request:
    types: [closed]
permissions:
  contents: read
  pull-requests: write
jobs:
  close:
    uses: nkdAgility/OpenGuidePlatform/.github/workflows/guide-site-close-pr.yaml@v0.5.3-Preview.6
    secrets:
      static-web-app-token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
